namespace SBERP.HumanResources.Enum
{
    /// <summary>
    /// Mirror of SBERP.Security.Enum.EnumResponseType — kept identical so the
    /// frontend's DataResponse contract works across services.
    /// </summary>
    public enum EnumResponseType
    {
        Success = 1,
        Warning = 2,
        Error = 3
    }
}
